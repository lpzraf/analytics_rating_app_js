<% provide(:title, "Home") %>

<div class="center jumbotron">
  <h1>Welcome to the Analytics Rating App</h1>

  <h2>An applicaion where you can add and rate analytics related courses!</h2>
  <div class="flex">
  <h3><%= link_to "Login", login_path, class: "btn btn-lg btn-primary" %></h3>
  &nbsp;&nbsp;
  <h3><%= link_to "Log In with Facebook", '/auth/facebook', class: "btn btn-lg btn-primary" %></h3>
  &nbsp;&nbsp;
  <h3><%= link_to "Sign Up", signup_path, class: "btn btn-lg btn-primary" %></h3>
  </div>
</div>
<%= link_to image_tag("rails.png", alt: "Rails logo"),
            'http://rubyonrails.org/' %>


            <% if session[:student_id] %>
              <h1><%= @student.first_name %></h1>
              <h2>Email: <%= @student.email %></h2>
              <h2>Facebook UID: <%= @student.uid %></h2>
              <img src="<%= @student.image %>">
            <% else %>
              <%= link_to('Log in with Facebook!', '/auth/facebook') %>
            <% end %>
